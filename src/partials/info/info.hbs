<div class="row">
  <p class="row__name">{{name}}</p>
  {{#if disabled}}
    <input type={{type}} class="row__value" disabled value="{{value}}">
  {{else}}
    <input type={{type}} class="row__value" value="{{value}}" placeholder={{placeholder}}>
  {{/if}}
</div>
